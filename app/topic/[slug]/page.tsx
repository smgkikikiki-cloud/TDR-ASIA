import { notFound } from "next/navigation";import { getTopic } from "@/lib/content";import { stories } from "@/content/stories";
export default async function TopicPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const t=getTopic(slug);if(!t)notFound();return <div className="shell page"><div className="eyebrow">{t.kicker} · TOPIC</div><h1>{t.name}</h1><div className="prose"><p>{t.description}</p><p>This page is permanent reference infrastructure. News events will strengthen it over time instead of replacing it.</p></div><div className="section"><div className="sectionHead"><h2>Related reporting</h2></div><div className="grid3">{stories.slice(0,3).map(s=><a className="card" href={`/story/${s.slug}`} key={s.slug}><div className="tag">{s.country}</div><h3>{s.title}</h3><p>{s.dek}</p></a>)}</div></div></div>}
