import { openFacts } from "@/content/facts";
export default function Open(){return <div className="shell page"><div className="eyebrow">MACHINE-READABLE EVIDENCE</div><h1>TDR Asia Open</h1><div className="prose"><p>The open layer stores atomic, source-backed facts that can reinforce reporting, reference pages, datasets and AI retrieval.</p></div><div className="list">{openFacts.map(f=><div className="fact" key={f.id}><div className="tag">{f.id}</div><h2>{f.claim}</h2><p>{f.country} · {f.sector} · {f.capability.join(" · ")}</p><div className="meta">Status: {f.status} · Updated {f.updatedAt}</div></div>)}</div><p><a href="/api/feed">JSON feed →</a></p></div>}
