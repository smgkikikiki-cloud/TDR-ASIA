export default function MegaNewsPage(){const items=[{t:'Project events from TDR Asia will land here',m:'Structured project_event records become the bridge between the news engine and the canonical project database.'},{t:'Official-source changes will become timeline events',m:'Status, value, owner and milestone changes can be proposed as mutations before publication.'},{t:'News remains a separate editorial surface',m:'TDR Asia keeps its own interface; Mega only receives normalized events and cross-links when useful.'}];return <section className="megaSection"><div className="megaShell"><div className="megaSectionHead"><div><div className="megaEyebrow">Event feed</div><h2>Project news</h2></div></div>{items.map((x,i)=><article className="megaNewsItem" key={i}><div className="megaPill">prototype</div><h3>{x.t}</h3><p className="megaMuted">{x.m}</p></article>)}</div></section>}
