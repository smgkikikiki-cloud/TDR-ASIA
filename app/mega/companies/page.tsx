import { getMegaCompanies } from '@/lib/mega-store';
export default async function CompaniesPage(){const companies=await getMegaCompanies();return <section className="megaSection"><div className="megaShell"><div className="megaSectionHead"><div><div className="megaEyebrow">Entity graph</div><h2>Companies</h2></div><span className="megaMuted">Derived from live project ownership</span></div><div className="megaGrid">{companies.map(c=><div className="megaCompany" key={c.name}><b>{c.name}</b><span>{c.type}</span><span>{c.projects} linked project{c.projects===1?'':'s'}</span></div>)}</div></div></section>}
